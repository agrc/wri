{
  "name": "@ugrc/wri-functions",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "scripts": {
    "build": "npm run build:shared && npm run clean && tsc",
    "build:shared": "npm run clean:shared && tsc -p ../packages/shared/tsconfig.json",
    "build:shared:watch": "npm run clean:shared && tsc -p ../packages/shared/tsconfig.json --watch",
    "build:watch": "npm run build:shared:watch && tsc --watch",
    "build:watch:only": "tsc --watch",
    "check": "npm run build:shared && tsc -b && tsc -p tsconfig.tools.json --noEmit",
    "check:proxy": "node --import tsx/esm scripts/check-db-proxy.ts",
    "clean": "rimraf lib",
    "clean:shared": "rimraf ../packages/shared/dist ../packages/shared/tsconfig.tsbuildinfo",
    "db:migrate:latest": "node --import tsx/esm scripts/migrate.ts latest",
    "db:migrate:make": "node --import tsx/esm ./node_modules/knex/bin/cli.js --knexfile knexfile.ts migrate:make",
    "db:migrate:rollback": "node --import tsx/esm scripts/migrate.ts rollback",
    "db:migrate:status": "node --import tsx/esm scripts/migrate.ts status",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint .",
    "logs": "firebase functions:log",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run serve",
    "start:local": "node --import tsx/esm scripts/start-local-dev.ts"
  },
  "dependencies": {
    "@arcgis/core": "^4.34.8",
    "@esri/arcgis-rest-feature-service": "^4.8.0",
    "@esri/arcgis-rest-request": "^4.8.0",
    "@google-cloud/cloud-sql-connector": "^1.9.0",
    "@ugrc/wri-shared": "file:../packages/shared",
    "firebase-admin": "^13.6.1",
    "firebase-functions": "^7.0.5",
    "knex": "^3.1.0",
    "ky": "^1.14.3",
    "tedious": "^19.2.0"
  },
  "devDependencies": {
    "eslint": "^9.39.2",
    "firebase-functions-test": "^3.4.1",
    "firebase-tools": "^15.5.1",
    "nodemon": "^3.1.11",
    "rimraf": "^6.1.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": "22"
  }
}
